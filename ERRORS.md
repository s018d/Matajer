# سجل أخطاء ضل — لا يُمسح أبداً

## 2026-09-17 — خطأ كسر كل صفحات المتجر (من شغلي)
- السبب: أضفت متغيرات `__safeOg/__safeColor/__safeCss` في `views/partials/head.ejs` بعد سطر `og:image` الذي يستخدمها — EJS نفذ السطر قبل التعريف فكسرت كل صفحة تستخدم القالب المشترك (المتاجر/الدخول/الخطأ).
- الحل: نقل كتلة التحقق لأول الملف قبل أي استخدام، وتوحيد `__safePwa` أيضاً.
- الدرس: أي متغير مشترك يُعرّف أول الملف دائماً، واختبار صفحة متجر حقيقية بعد أي تعديل على القوالب المشتركة — لا يُكرر.
